#!/bin/bash

#
# The super awesome timetable generator template!
#
# Copy this file into the current week, name it reg_yourname.sh (no whitespace)
# Whole numbers (hours), no whitespace
#
# If you like to separate days you can write something like this:
#
#	let reg_pre_study=25*20*2*1*4
#
# That would be 25 hours monday, 20 tuesday, 2 we... etc.
#

let reg_pre_study=30
let reg_requirement=29
let reg_implementation=0
